/**
 * LÉZARD TEXTE — единственное место с адресом AI-прокси.
 *
 * Домен меняется здесь одной строкой.
 * Секрет DeepSeek живёт только на сервере
 * (/home/c/cv038824/.seferometer-secrets/deepseek.key)
 * и никогда не попадает в APK.
 */
export const PROXY_URL = 'https://cv038824.tw1.ru/deep-seek/deep-seek.php';

/** Таймаут AI-запроса, мс */
export const AI_TIMEOUT_MS = 45000;
